cmake_minimum_required(VERSION 3.10...3.30)

#Note: Before Push/PR do not forgot to change version
project(CS2_cfgmaker_github VERSION 1.7)

set(SRC

    src/main.cpp
    src/action_start/start.cpp
    src/action_start/search.cpp
    src/action_instruction/instruction.cpp

)
#----------------------
add_library(functions
    include/clearwindow.cpp
    include/Localization.cpp
    include/return_standart.cpp
    include/browser.cpp
)
target_include_directories(functions PUBLIC include/headers)

add_executable(cfgmaker ${SRC})

target_link_libraries(cfgmaker PRIVATE functions)